server:
  port: 8080

spring:
  application:
    name: mediflowiq

  # ─────────────────────────────────────────────
  # PostgreSQL Database (Phase 6+)
  # ─────────────────────────────────────────────
  datasource:
    url: jdbc:postgresql://db.zuqizpcptvshptbgptmw.supabase.co:5432/postgres
    username: postgres
    password: yJ?!B/@rNjFHB5H
    driver-class-name: org.postgresql.Driver


  

  # ─────────────────────────────────────────────
  # H2 In-Memory Database (Phase 1–5 — Dev Only)
  # Kept here for reference — do not re-enable unless reverting to dev mode
  # ─────────────────────────────────────────────
  # datasource:
  #   url: jdbc:h2:mem:mediflowiq;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password:
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update          # keeps existing data; creates new tables automatically
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC

  # ── Scheduling thread pool for ambulance simulation ───────────────
  task:
    scheduling:
      pool:
        size: 5

logging:
  level:
    com.mediflowiq: DEBUG
    org.springframework.web.socket: INFO
    org.springframework.security: INFO

# ─────────────────────────────────────────────
# Phase 4 — JWT Security Configuration
# ─────────────────────────────────────────────
app:
  jwt:
    secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
    expiration-ms: 3600000       # 1 hour
    refresh-expiration-ms: 86400000  # 24 hours

# ─────────────────────────────────────────────
# Phase 6 — Razorpay Payment Gateway
# Keys are loaded from environment variables — NEVER hardcode here.
# Set RAZORPAY_KEY_ID and RAZORPAY_KEY_SECRET in your OS env or .env file.
# ─────────────────────────────────────────────
razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_placeholder}
  key-secret: ${RAZORPAY_KEY_SECRET:placeholder_secret}

