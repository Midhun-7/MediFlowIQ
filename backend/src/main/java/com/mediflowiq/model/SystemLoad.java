package com.mediflowiq.model;

public enum SystemLoad {
    LOW,      // Smooth operations
    MODERATE, // Busy but manageable
    HIGH,     // Near capacity, potential delays
    CRITICAL  // Exceeds capacity, needs immediate attention
}
